name: build simplex-chat for linux

on:
  workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - run: git clone https://github.com/simplex-chat/simplex-chat
      - run: cd simplex-chat
      - run: git checkout stable
      - run: DOCKER_BUILDKIT=1 docker build --output . .

